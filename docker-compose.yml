version: '3.4'

services:
  nats:
    image: nats-streaming:latest
    entrypoint:
      - /nats-streaming-server
      - -cid
      - crypto-cluster
    ports:
      - "4222:4222"
      - "8222:8222"
    restart: always
    tty: true

  marketdepth.pub:
    image: ${DOCKER_REGISTRY-}marketdepthpub
    build:
      context: .
      dockerfile: src/MarketDepth.Pub/Dockerfile
    env_file:
      - .env
    depends_on:
      - nats

  marketdepth.ws:
    image: ${DOCKER_REGISTRY-}marketdepthsubwss
    build:
      context: .
      dockerfile: src/MarketDepth.Sub.WsServer/Dockerfile
    expose:
      - "5050"
    ports:
      - "5050:5050"
    env_file:
      - .env
    depends_on:
      - nats